# Enhanced validation with better error messages

Enhanced validation with better error messages

## Usage

``` r
assert_valid_methy_samples(data_path, samples, sample_check_limit = 1000)
```

## Arguments

- data_path:

  path to methylation data file

- samples:

  sample annotation data.frame

- sample_check_limit:

  number of entries to check for sample matching
